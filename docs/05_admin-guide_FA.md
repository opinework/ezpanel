# راهنمای مدیر

**زبان**: [English](./05_admin-guide.md) | [简体中文](./05_admin-guide_CN.md) | [繁體中文](./05_admin-guide_TW.md) | [Русский](./05_admin-guide_RU.md) | فارسی

این سند برای مدیران EzPanel است و روش کار با ماژول‌های مختلف پنل مدیریت را توضیح می‌دهد.

![پنل مدیریت](../images/dashboard.png)

---

## اولین ورود

اطلاعات ورود پیش‌فرض مدیر:

- **ایمیل**: `admin@opine.work`
- **رمز عبور**: `admin123`

> **هشدار امنیتی**: پس از ورود، فوراً در «تنظیمات شخصی» رمز عبور و ایمیل پیش‌فرض را تغییر دهید و تأیید دو مرحله‌ای را فعال کنید.

---

## مدیریت کاربران

به **مدیریت کاربران ← لیست کاربران** بروید:

### مشاهده کاربران

| عملیات | توضیح |
|--------|-------|
| جستجو | جستجو بر اساس ایمیل یا شناسه کاربر |
| فیلتر | فیلتر بر اساس وضعیت (عادی / مسدود) و پلان |
| خروجی | خروجی داده‌های کاربران به فرمت CSV / Excel |

### عملیات روی کاربران

- **مشاهده جزئیات**: مشاهده اشتراک، سفارش‌ها، ترافیک، موجودی و سایر اطلاعات کاربر
- **ویرایش**: تغییر ایمیل، رمز عبور، موجودی، حد ترافیک، تاریخ انقضا
- **مسدود / رفع مسدود**: جلوگیری / اجازه به کاربر برای ورود و استفاده از اشتراک
- **شارژ موجودی**: شارژ مستقیم حساب کاربر

### بازنشانی دستی رمز عبور

در جزئیات کاربر می‌توانید مستقیماً رمز عبور جدید تنظیم کنید بدون نیاز به تأیید رمز قدیمی.

---

## مدیریت پلان‌ها

به **مدیریت پلان** بروید:

### ایجاد پلان

| فیلد | توضیح |
|-----|-------|
| نام پلان | نامی که به کاربران نمایش داده می‌شود |
| حد ترافیک | ترافیک قابل استفاده در هر دوره صدور صورتحساب (گیگابایت) |
| دوره صدور صورتحساب | ماهانه / سه‌ماهه / شش‌ماهه / سالانه؛ برای هر دوره قیمت جداگانه تنظیم می‌شود |
| موجودی | -1 یعنی نامحدود؛ عدد صحیح مثبت محدودیت موجودی تعیین می‌کند |
| محدودیت دستگاه | تعداد دستگاه‌های همزمان آنلاین؛ 0 یعنی بدون محدودیت |
| محدودیت سرعت | سرعت به مگابیت‌بر‌ثانیه؛ 0 یعنی بدون محدودیت |
| گروه گره | گروه گره‌های قابل دسترس با این پلان |
| مرتب‌سازی | عدد کمتر = بالاتر در لیست |
| وضعیت | منتشرشده / پنهان |

### نمایش‌پذیری پلان

پلان‌های پنهان در سمت کاربر نمایش داده نمی‌شوند اما کاربرانی که قبلاً خریداری کرده‌اند تحت تأثیر قرار نمی‌گیرند.

---

## مدیریت گره‌ها

به **مدیریت سیستم ← مدیریت گره** بروید:

### ایجاد گره

**پیکربندی پایه**:

| فیلد | توضیح |
|-----|-------|
| نام گره | به کاربران نمایش داده می‌شود؛ از Emoji پشتیبانی می‌کند، مثلاً «🇭🇰 هنگ‌کنگ ۰۱» |
| آدرس گره | آدرس IP یا نام دامنه سرور گره |
| نوع گره | XrayM (توصیه‌شده) / XrayR |
| گروه گره | تعلق به گروه؛ بر دسترسی پلان‌ها تأثیر می‌گذارد |
| محدودیت سرعت | محدودیت جهانی (مگابیت‌بر‌ثانیه)؛ 0 بدون محدودیت |
| محدودیت دستگاه | تعداد دستگاه‌های همزمان آنلاین؛ 0 بدون محدودیت |
| مرتب‌سازی | عدد کمتر = بالاتر در لیست |

**پیکربندی پروتکل (XrayM)**:

روی «افزودن سریع پروتکل» کلیک کنید تا ترکیب پیش‌تنظیم را انتخاب کنید یا پروتکل‌ها را به‌صورت دستی اضافه کنید:

- **لایه انتقال**: TCP / WebSocket / gRPC / HTTPUpgrade
- **لایه امنیت**: TLS / REALITY / بدون
- **پورت**: پورت مستقل برای هر پروتکل

پس از پیکربندی، شناسه گره و کلید ارتباطی را در فایل پیکربندی XrayM روی سرور وارد کنید. جزئیات در [اتصال گره](./03_node-setup_FA.md).

### مانیتورینگ گره

به **مدیریت سیستم ← بررسی سلامت** بروید تا وضعیت بلادرنگ تمام گره‌ها را ببینید:

- وضعیت آنلاین / آفلاین گره
- مصرف CPU، حافظه، دیسک
- سرعت شبکه بلادرنگ
- تعداد کاربران آنلاین فعلی

برای مشاهده نمودارهای تاریخی تفصیلی روی گره کلیک کنید؛ داده‌ها هر ۱۰ ثانیه به‌صورت خودکار به‌روز می‌شوند.

> مانیتورینگ گره فقط برای XrayM پشتیبانی می‌شود. XrayR فقط داده‌های ترافیک را ارسال می‌کند.

---

## مدیریت سفارش‌ها (نسخه Pro)

به **مدیریت سفارش** بروید:

| عملیات | توضیح |
|--------|-------|
| مشاهده سفارش‌ها | فیلتر بر اساس کاربر، وضعیت، زمان |
| جزئیات سفارش | مشاهده سوابق پرداخت، اطلاعات پلان |
| استرداد | استرداد مبلغ سفارش به موجودی کاربر |
| لغو | لغو سفارش‌های پرداخت‌نشده |
| تکمیل دستی | علامت‌گذاری دستی سفارش‌های پرداخت آفلاین به‌عنوان تکمیل‌شده |

---

## مدیریت کوپن (نسخه Pro)

به **بازاریابی ← کوپن‌ها** بروید:

| فیلد | توضیح |
|-----|-------|
| نوع تخفیف | مبلغ ثابت / درصد تخفیف |
| محدودیت استفاده | تعداد کل استفاده / تعداد استفاده هر فرد |
| پلان‌های قابل اعمال | خالی بگذارید تا برای همه پلان‌ها اعمال شود |
| مدت اعتبار | تاریخ شروع / پایان |
| تولید انبوه | می‌توان چندین کوپن با کدهای مختلف به‌صورت انبوه تولید کرد |

---

## مدیریت تیکت (نسخه Pro)

به **مدیریت تیکت** بروید:

- مشاهده همه تیکت‌های ارسال‌شده توسط کاربران
- فیلتر بر اساس وضعیت (در انتظار / در حال بررسی / بسته‌شده)
- پاسخ به تیکت‌ها با پشتیبانی از متن غنی
- بستن یا بازگشایی مجدد تیکت‌ها

---

## پایگاه دانش (نسخه Pro)

به **پایگاه دانش** بروید:

- ایجاد مقالات FAQ با پشتیبانی از Markdown / متن غنی
- مدیریت دسته‌بندی
- تنظیم محدوده نمایش مقاله (همه کاربران / فقط کاربران پولی)
- کاربران می‌توانند در صفحه «راهنما» مطالب را مشاهده کنند

---

## سیستم ایمیل

### پیکربندی SMTP

به **تنظیمات سیستم ← تنظیمات ایمیل** بروید:

| گزینه | توضیح |
|-------|-------|
| سرور SMTP | مثلاً `smtp.gmail.com` |
| پورت | معمولاً 465 (SSL) یا 587 (STARTTLS) |
| نام کاربری | آدرس ایمیل فرستنده |
| رمز عبور | رمز ایمیل یا رمز ویژه برنامه |
| نام فرستنده | نامی که به گیرندگان نمایش داده می‌شود |

پس از پر کردن، روی «ارسال ایمیل آزمایشی» کلیک کنید تا پیکربندی تأیید شود.

### قالب‌های ایمیل

به **مدیریت سیستم ← قالب‌های ایمیل** بروید تا ایمیل‌های زیر را سفارشی کنید:

- ایمیل تأیید ثبت‌نام
- ایمیل بازنشانی رمز عبور
- اعلان سفارش
- هشدار ترافیک
- یادآوری انقضای پلان

**نیاز به بازنشانی قالب‌ها پس از تغییر زبان**: پس از تغییر زبان رابط کاربری، روی «بازنشانی به پیش‌فرض» کلیک کنید — قالب‌های ایمیل به نسخه زبان انتخاب‌شده به‌روز می‌شوند.

> بازنشانی محتوای سفارشی را بازنویسی می‌کند — از قبل نسخه پشتیبان تهیه کنید.

---

## تنظیمات سیستم

به **مدیریت سیستم ← تنظیمات سیستم** بروید:

| دسته‌بندی | گزینه‌های پیکربندی |
|----------|-------------------|
| تنظیمات پایه | نام سایت، لوگو، آدرس سایت |
| تنظیمات ثبت‌نام | ثبت‌نام باز، تأیید ایمیل، نیاز به کد دعوت |
| تنظیمات اشتراک | URL اشتراک، پیکربندی فرمت اشتراک |
| تنظیمات گره | کلید API گره |
| تنظیمات امنیتی | قفل شدن پس از خطاهای ورود، لیست سیاه IP |

### تغییر کلید API گره

پس از تغییر کلید، باید `ApiKey` را در فایل‌های پیکربندی XrayM/XrayR روی تمام گره‌ها به‌طور همزمان به‌روز کنید — در غیر این صورت گره‌ها نمی‌توانند متصل شوند.

---

## خروجی داده

صفحات زیر از خروجی داده پشتیبانی می‌کنند:

- لیست کاربران ← خروجی CSV
- لیست سفارش‌ها ← خروجی CSV / Excel
- گزارش‌های مالی ← خروجی

---

## لاگ حسابرسی

به **مدیریت سیستم ← لاگ حسابرسی** بروید تا مشاهده کنید:

- سوابق ورود کاربران (IP، زمان)
- سوابق عملیات مدیران
- عملیات کلیدی (تغییر رمز عبور، تغییر موجودی و غیره)

---

## تغییر زبان و بازنشانی قالب‌ها

1. روی تغییردهنده زبان در گوشه بالا-راست کلیک کنید و زبان مورد نظر را انتخاب کنید
2. به **مدیریت سیستم ← قالب‌های ایمیل** بروید و روی «بازنشانی به پیش‌فرض» کلیک کنید
3. به **مدیریت سیستم ← قالب‌های اشتراک** بروید و روی «بازنشانی به پیش‌فرض» کلیک کنید

پس از این مراحل، تمام محتوای قالب‌ها به نسخه زبان انتخاب‌شده به‌روز می‌شود.
